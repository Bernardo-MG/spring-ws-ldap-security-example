###############################################################################
#                             APPLICATION CONFIG                              #
###############################################################################

# Development options
# debug: true

# LDAP
security:
  ldap:
    url:
    base:
    pattern:

spring:
  # AOP
  aop:
    proxy-target-class: false

# Management
management:
  endpoints:
    web:
      exposure:
        include: health,auditevents
    enabled-by-default: false
    auditevents:
      enabled: true
  endpoint:
    health:
      enabled: true